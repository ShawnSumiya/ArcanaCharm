# Members CMS

この文書は開発者向けです。ファン向けのページには、取得処理の詳細を出しません。

Members は、まず `index.html` の静的なカードを表示します。JavaScript が公開済みメンバーを背景で取得し、データと写真がすべて確認できたときだけ、そのカードへ差し替えます。取得、検証、タイムアウト、写真の準備のいずれかが失敗したときは、静的なカードを残します。

0件の配列は、通信失敗ではなく、公開側で表示対象がいない状態です。そのときだけ、カードを空にします。見出しは残します。

下書きの表や非公開の写真は読みません。使う鍵は公開用の publishable key だけです。Secret Key と Service Role Key は置かないでください。

## CMS を止める

問題が起きたときは、`members-public-config.js` の `membersCmsEnabled` を `false` にします。ページは静的な Members のままになり、取得もしません。

本番のサイトへこの変更を反映するのは、Preview の確認と別の承認が済んでからです。
